import React from "react";
import { render, screen } from "@testing-library/react";
import App from "App";

test("renders TaskCircle", () => {
  // render(<App />);
  // const linkElement = screen.getByText(/TaskCircle/i);
  // expect(linkElement).toBeInTheDocument();
});
